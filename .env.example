# Copy to .env and fill in.
#
# Required for L3+ (re-derive log-odds / re-run rollouts):
#   HF_TOKEN          gated access for google/gemma-4-31B-it
#                     (request at https://huggingface.co/google/gemma-4-31B-it)
#   OPENAI_API_KEY    GPT-5.4-nano calls for the bias judge + rollout judge
#
# Not needed for L1 (re-render plots from committed artefacts).

HF_TOKEN=
OPENAI_API_KEY=
